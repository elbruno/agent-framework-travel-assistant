"""Context package for seed data and initialization utilities.

Files
- `seed.json`: Defines initial users and their durable memories to be inserted
  into Mem0 on first run. The app reads this file to:
  - Pick a default user tab in the UI
  - Preload user-specific memories so the agent can personalize immediately
"""

